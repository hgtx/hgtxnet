(function ($) { 'use strict'; var navTarget = $('body').attr('data-page-url'); var navSourcePath = null; var docTitle = document.title; var History = window.History; var loadingFallbackTimer = null; function getSafePageTitle() { return docTitle || document.title || ''; } function pushPageState(url) { if ( !url || url === 'undefined' ) { return false; } History.pushState(null, getSafePageTitle(), url); return true; } function getAjaxPageLoadUrl() { return window.location.pathname; } function extractPageContentFromHtml(html) { var $parsed = $('<div>').append($.parseHTML(html, document, true)); return $parsed.find('.page__content').first(); } function clearLoadingFallbackTimer() { if ( loadingFallbackTimer ) { window.clearTimeout(loadingFallbackTimer); loadingFallbackTimer = null; } } function scheduleLoadingFallback() { clearLoadingFallbackTimer(); loadingFallbackTimer = window.setTimeout(function() { loadingFallbackTimer = null; $('body').removeClass('loading menu--open sidebar-exit-animating sidebar-exit-active'); }, 12000); } function loadAjaxPageContent(loadUrl, $loader, callback) { $.ajax({ url: loadUrl, dataType: 'html', cache: true }).done(function(html) { var $content = extractPageContentFromHtml(html); if ( !$content.length ) { callback('error'); return; } $loader.empty().append($content); callback('success'); }).fail(function() { callback('error'); }); } var $galleryLightbox = $( '<div class="gallery-lightbox" aria-hidden="true">' + '<div class="gallery-lightbox__overlay"></div>' + '<div class="gallery-lightbox__viewport">' + '<div class="gallery-lightbox__track is-resetting">' + '<div class="gallery-lightbox__slide">' + '<img class="gallery-lightbox__image" alt="">' + '</div>' + '</div>' + '</div>' + '</div>' ); $galleryLightbox.appendTo('body'); var galleryLightboxState = { images: [], index: -1 }; var galleryLightboxAnimating = false; var galleryLightboxTouchStartX = 0; var galleryLightboxTouchStartY = 0; var galleryLightboxDidSwipe = false; var galleryLightboxCloseCallback = null; var galleryLightboxOpenOnLoad = false; var galleryLightboxPreviousBodyBackground = null; var galleryThumbTouchStartX = 0; var galleryThumbTouchStartY = 0; var portfolioTouchStartX = 0; var portfolioTouchStartY = 0; function getGalleryLightboxTrack() { return $galleryLightbox.find('.gallery-lightbox__track'); } function getGalleryLightboxImage() { return $galleryLightbox.find('.gallery-lightbox__image'); } function normalizeGalleryIndex(index) { var total = galleryLightboxState.images.length; if ( index < 0 ) { index = total - 1; } if ( index >= total ) { index = 0; } return index; } function loadGalleryLightboxImage($image, src, callback) { $image.one('load', callback); $image.attr('src', src); if ( $image[0].complete ) { callback(); } } function forceGalleryLightboxReflow($element) { return $element[0].offsetHeight; } function setGalleryLightboxSingleSlide(src, callback) { var $track = getGalleryLightboxTrack(); $track .removeClass('is-animating') .addClass('is-resetting') .css('transform', 'translate3d(0, 0, 0)') .html('<div class="gallery-lightbox__slide"><img class="gallery-lightbox__image" alt=""></div>'); loadGalleryLightboxImage($track.find('.gallery-lightbox__image'), src, function() { $track.removeClass('is-resetting'); if ( callback ) { callback(); } }); } function resetGalleryLightboxSlides() { galleryLightboxAnimating = false; getGalleryLightboxTrack() .removeClass('is-animating is-resetting') .css('transform', 'translate3d(0, 0, 0)') .html('<div class="gallery-lightbox__slide"><img class="gallery-lightbox__image" alt=""></div>'); getGalleryLightboxImage().attr('src', ''); } function animateGalleryLightboxSlide(index, direction) { if ( galleryLightboxAnimating || !galleryLightboxState.images.length ) { return; } var oldIndex = galleryLightboxState.index; index = normalizeGalleryIndex(index); if ( index === oldIndex ) { return; } var outgoingSrc = galleryLightboxState.images[oldIndex]; var incomingSrc = galleryLightboxState.images[index]; var $track = getGalleryLightboxTrack(); var trackHtml; galleryLightboxAnimating = true; if ( direction > 0 ) { trackHtml = '<div class="gallery-lightbox__slide"><img class="gallery-lightbox__image" alt=""></div>' + '<div class="gallery-lightbox__slide"><img class="gallery-lightbox__image" alt=""></div>'; $track .removeClass('is-animating') .addClass('is-resetting') .css('transform', 'translate3d(0, 0, 0)') .html(trackHtml); $track.find('.gallery-lightbox__slide').eq(0).find('img').attr('src', outgoingSrc); } else { trackHtml = '<div class="gallery-lightbox__slide"><img class="gallery-lightbox__image" alt=""></div>' + '<div class="gallery-lightbox__slide"><img class="gallery-lightbox__image" alt=""></div>'; $track .removeClass('is-animating') .addClass('is-resetting') .css('transform', 'translate3d(-100vw, 0, 0)') .html(trackHtml); $track.find('.gallery-lightbox__slide').eq(1).find('img').attr('src', outgoingSrc); } var $incomingImage = direction > 0 ? $track.find('.gallery-lightbox__slide').eq(1).find('img') : $track.find('.gallery-lightbox__slide').eq(0).find('img'); loadGalleryLightboxImage($incomingImage, incomingSrc, function() { forceGalleryLightboxReflow($track); $track.removeClass('is-resetting').addClass('is-animating'); if ( direction > 0 ) { $track.css('transform', 'translate3d(-100vw, 0, 0)'); } else { $track.css('transform', 'translate3d(0, 0, 0)'); } galleryLightboxState.index = index; var finished = false; var finishSlide = function(event) { if ( event && event.target !== $track[0] ) { return; } if ( finished ) { return; } finished = true; $track.off('transitionend webkitTransitionEnd', finishSlide); setGalleryLightboxSingleSlide(incomingSrc); galleryLightboxAnimating = false; }; $track.on('transitionend webkitTransitionEnd', finishSlide); window.setTimeout(finishSlide, 650); }); } function showGalleryLightboxImage(index, direction) { if ( direction === 0 ) { $galleryLightbox.addClass('gallery-lightbox--loading'); setGalleryLightboxSingleSlide(galleryLightboxState.images[index], function() { $galleryLightbox.removeClass('gallery-lightbox--loading'); }); return; } animateGalleryLightboxSlide(index, direction); } function applyGalleryLightboxBackground() { var $body = $('body'); galleryLightboxPreviousBodyBackground = $body[0].style.backgroundColor || ''; $body.css('background-color', '#ffffff'); } function restoreGalleryLightboxBackground() { var $body = $('body'); if ( galleryLightboxPreviousBodyBackground !== null ) { $body.css('background-color', galleryLightboxPreviousBodyBackground); galleryLightboxPreviousBodyBackground = null; } } function openGalleryLightbox(imageSrc, $link) { galleryLightboxState.images = getGalleryImages($link); galleryLightboxState.index = galleryLightboxState.images.indexOf(imageSrc); if ( galleryLightboxState.index < 0 ) { galleryLightboxState.index = 0; } resetGalleryLightboxSlides(); $galleryLightbox .addClass('gallery-lightbox--open') .attr('aria-hidden', 'false'); $('body').addClass('gallery-lightbox-active'); applyGalleryLightboxBackground(); showGalleryLightboxImage(galleryLightboxState.index, 0); } function navigateGalleryLightbox(delta) { if ( !galleryLightboxState.images.length || galleryLightboxAnimating ) { return; } showGalleryLightboxImage(galleryLightboxState.index + delta, delta); } function finalizeGalleryLightboxClose() { $galleryLightbox .removeClass('gallery-lightbox--open gallery-lightbox--loading gallery-lightbox--closing') .attr('aria-hidden', 'true'); resetGalleryLightboxSlides(); $('body').removeClass('gallery-lightbox-active'); restoreGalleryLightboxBackground(); galleryLightboxState.images = []; galleryLightboxState.index = -1; galleryLightboxAnimating = false; if ( galleryLightboxCloseCallback ) { var callback = galleryLightboxCloseCallback; galleryLightboxCloseCallback = null; callback(); } } function closeGalleryLightbox(animated, callback) { if ( galleryLightboxAnimating || !isGalleryLightboxOpen() ) { return; } galleryLightboxCloseCallback = callback || null; if ( !animated ) { finalizeGalleryLightboxClose(); return; } var $track = getGalleryLightboxTrack(); galleryLightboxAnimating = true; $galleryLightbox.addClass('gallery-lightbox--closing'); forceGalleryLightboxReflow($track); $track .removeClass('is-resetting is-closing') .addClass('is-animating is-closing') .css('transform', 'translate3d(0, 100vh, 0)'); var finished = false; var finishClose = function(event) { if ( event && event.target !== $track[0] ) { return; } if ( finished ) { return; } finished = true; $track.off('transitionend webkitTransitionEnd', finishClose); finalizeGalleryLightboxClose(); }; $track.on('transitionend webkitTransitionEnd', finishClose); window.setTimeout(finishClose, 650); } function getGalleryImages($link) { return $link.closest('.gallery--grid').find('.gallery__item__link').map(function() { return $(this).attr('href'); }).get(); } function isGalleryLightboxOpen() { return $galleryLightbox.hasClass('gallery-lightbox--open'); } function normalizeProjectPath(path) { if ( !path || path === '/' ) { return '/'; } if ( path.length > 1 && path.charAt(path.length - 1) === '/' ) { return path.slice(0, -1); } return path; } function getProjectUrls() { return $('.projects-nav [data-project-url]').map(function() { return normalizeProjectPath($(this).attr('href')); }).get(); } function getNextProjectUrl() { var projectUrls = getProjectUrls(); if ( !projectUrls.length ) { return null; } var currentPath = normalizeProjectPath(navTarget || window.location.pathname); var currentIndex = projectUrls.indexOf(currentPath); if ( currentIndex < 0 ) { return projectUrls[0]; } return projectUrls[(currentIndex + 1) % projectUrls.length]; } function getPreviousProjectUrl() { var projectUrls = getProjectUrls(); if ( !projectUrls.length ) { return null; } var currentPath = normalizeProjectPath(navTarget || window.location.pathname); var currentIndex = projectUrls.indexOf(currentPath); if ( currentIndex < 0 ) { return projectUrls[projectUrls.length - 1]; } return projectUrls[(currentIndex - 1 + projectUrls.length) % projectUrls.length]; } function isCoverPage() { var path = normalizeProjectPath(navTarget || window.location.pathname); return path === '/' || $('.page__content .cover').length > 0; } function isProjectsListingPage() { var path = normalizeProjectPath(navTarget || window.location.pathname); return path === '/projects' || $('.page__content .portfolio-wrap').length > 0; } function isMainMenuPage() { var currentPath = normalizeProjectPath(navTarget || window.location.pathname); return getMainPageUrls().indexOf(currentPath) >= 0; } function isMainNavigationContext() { return isCoverPage() || isMainMenuPage(); } function isProjectGalleryPage() { var path = normalizeProjectPath(navTarget || window.location.pathname); if ( path.indexOf('/project/') !== 0 ) { return false; } return hasPageGridGallery(); } function hasPageGridGallery() { return $('.page__content .gallery--grid .gallery__item__link').length > 0; } function getMainPageUrls() { return $('.menu .menu__list__item__link').map(function() { return normalizeProjectPath($(this).attr('href')); }).get(); } function navigateMainPage(delta) { if ( galleryLightboxAnimating || isGalleryLightboxOpen() ) { return false; } if ( !isMainNavigationContext() ) { return false; } var mainPageUrls = getMainPageUrls(); var mainPageUrl; if ( !mainPageUrls.length ) { return false; } var currentPath = normalizeProjectPath(navTarget || window.location.pathname); var currentIndex = mainPageUrls.indexOf(currentPath); if ( currentIndex < 0 ) { if ( !isCoverPage() ) { return false; } mainPageUrl = delta > 0 ? mainPageUrls[0] : mainPageUrls[mainPageUrls.length - 1]; } else { mainPageUrl = mainPageUrls[(currentIndex + delta + mainPageUrls.length) % mainPageUrls.length]; } navSourcePath = normalizeProjectPath(navTarget || window.location.pathname); navTarget = mainPageUrl; pushPageState(mainPageUrl); return true; } function getParentPageUrl() { var path = normalizeProjectPath(navTarget || window.location.pathname); if ( path === '/' ) { return null; } if ( isMainMenuPage() ) { return '/'; } if ( path.indexOf('/project/') === 0 ) { return '/projects'; } return null; } function navigateUpHierarchy() { if ( galleryLightboxAnimating || isGalleryLightboxOpen() ) { return false; } var parentUrl = getParentPageUrl(); if ( !parentUrl ) { return false; } navSourcePath = normalizeProjectPath(navTarget || window.location.pathname); navTarget = parentUrl; pushPageState(parentUrl); return true; } function getChildPageUrl() { if ( isCoverPage() ) { return '/projects'; } if ( isProjectsListingPage() ) { var projectUrls = getProjectUrls(); if ( !projectUrls.length ) { return null; } return projectUrls[0]; } return null; } function navigateDownHierarchy() { if ( galleryLightboxAnimating || isGalleryLightboxOpen() ) { return false; } var childUrl = getChildPageUrl(); if ( !childUrl ) { return false; } navSourcePath = normalizeProjectPath(navTarget || window.location.pathname); navTarget = childUrl; pushPageState(childUrl); return true; } function randomizeCoverImage(callback) { var $cover = $('.page__content .cover'); if ( !$cover.length ) { if ( callback ) { callback(false); } return; } var imagesJson = $cover.attr('data-cover-images'); var $coverImage = $cover.find('.cover__image'); $cover.removeClass('cover--ready'); if ( !imagesJson ) { $cover.addClass('cover--ready'); if ( callback ) { callback(true, $coverImage[0]); } return; } var images; try { images = JSON.parse(imagesJson); } catch ( error ) { $cover.addClass('cover--ready'); if ( callback ) { callback(true, $coverImage[0]); } return; } if ( !images.length ) { $cover.addClass('cover--ready'); if ( callback ) { callback(true, $coverImage[0]); } return; } var image = images[Math.floor(Math.random() * images.length)]; var preload = new Image(); preload.onload = preload.onerror = function() { $coverImage.attr('src', image); requestAnimationFrame(function() { $cover.addClass('cover--ready'); updateCoverBrandingColor($coverImage[0]); if ( callback ) { callback(true, $coverImage[0]); } }); }; preload.src = image; } function revealPageContent() { clearLoadingFallbackTimer(); var $portfolioFit = $('.portfolio--fit'); if ( $portfolioFit.length ) { $portfolioFit.find('.portfolio-wrap').imagesLoaded(function() { layoutPortfolioFit(); $('body').removeClass('loading menu--open'); }); } else if ( $('.portfolio-wrap').length ) { $('.portfolio-wrap').imagesLoaded( function() { $('.portfolio-wrap').masonry({ itemSelector: '.portfolio-item', transitionDuration: 0 }); $('body').removeClass('loading menu--open'); }); } else { $('body').removeClass('loading menu--open'); } $('.blog-wrap').imagesLoaded( function() { $('.blog-wrap').masonry({ itemSelector: '.blog-post', transitionDuration: 0 }); }); } var portfolioFitResizeTimer = null; function getPortfolioItemAspectRatios($wrap) { return $wrap.find('.portfolio-item').map(function() { var img = $(this).find('img')[0]; if ( img && img.naturalWidth && img.naturalHeight ) { return img.naturalWidth / img.naturalHeight; } return 1; }).get(); } function simulatePortfolioRows(aspects, imageHeight, gap, availWidth) { var rows = []; var currentRow = []; var currentWidth = 0; for ( var i = 0; i < aspects.length; i++ ) { var itemWidth = imageHeight * aspects[i]; var additionalWidth = currentRow.length ? gap + itemWidth : itemWidth; if ( currentRow.length && currentWidth + additionalWidth > availWidth + 0.5 ) { rows.push(currentRow); currentRow = [i]; currentWidth = itemWidth; } else { currentRow.push(i); currentWidth += additionalWidth; } } if ( currentRow.length ) { rows.push(currentRow); } return rows; } function measurePortfolioRowsHeight(rows, imageHeight, captionHeight, gap) { if ( !rows.length ) { return 0; } return rows.length * (imageHeight + captionHeight) + gap * (rows.length - 1); } function findEqualPortfolioImageHeight(aspects, gap, availWidth, availHeight, captionHeight) { var low = 40; var high = Math.max(low, availHeight - captionHeight); var bestHeight = low; while ( low <= high ) { var mid = Math.floor((low + high) / 2); var rows = simulatePortfolioRows(aspects, mid, gap, availWidth); var totalHeight = measurePortfolioRowsHeight(rows, mid, captionHeight, gap); if ( totalHeight <= availHeight ) { bestHeight = mid; low = mid + 1; } else { high = mid - 1; } } return bestHeight; } function resetPortfolioItemSizes($wrap) { $wrap.find('.portfolio-item').css({ width: '', flex: '' }); $wrap.find('.portfolio-item__image').css({ width: '', height: '', overflow: '' }); $wrap.find('.portfolio-item__image img').css({ width: '', height: '', maxWidth: '', marginLeft: '', marginTop: '' }); } function applyPortfolioEqualHeightLayout($wrap, imageHeight, aspects) { $wrap.find('.portfolio-item').each(function(index) { var $item = $(this); var itemWidth = imageHeight * aspects[index]; $item.css({ width: itemWidth + 'px', flex: '0 0 auto' }); $item.find('.portfolio-item__image').css({ width: itemWidth + 'px', height: imageHeight + 'px', overflow: '' }); $item.find('.portfolio-item__image img').css({ width: itemWidth + 'px', height: imageHeight + 'px', maxWidth: '', marginLeft: '', marginTop: '' }); }); } function measurePortfolioCaptionHeight($wrap) { var captionHeight = 0; $wrap.find('.portfolio-item__caption').each(function() { captionHeight = Math.max(captionHeight, $(this).outerHeight(true)); }); return captionHeight || 32; } function layoutPortfolioFit() { var $portfolio = $('.portfolio--fit'); if ( !$portfolio.length ) { $('body').removeClass('is-projects-page').css('overflow', ''); return; } var $wrap = $portfolio.find('.portfolio-wrap'); var count = $wrap.find('.portfolio-item').length; if ( !count ) { return; } $('body').addClass('is-projects-page'); var gapValue = parseFloat($wrap.css('column-gap')); if ( isNaN(gapValue) ) { gapValue = parseFloat($wrap.css('gap')); } if ( isNaN(gapValue) ) { gapValue = 20; } var portfolioStyles = window.getComputedStyle($portfolio[0]); var paddingBottom = parseFloat(portfolioStyles.paddingBottom) || 0; var bottomInset = 16; var availWidth = $wrap.innerWidth(); var $footer = $('.footer:visible'); var maxBottom = window.innerHeight - bottomInset; if ( $footer.length ) { var footerTop = $footer[0].getBoundingClientRect().top; if ( footerTop < window.innerHeight ) { maxBottom = Math.min(maxBottom, footerTop - paddingBottom); } } var wrapTop = $wrap[0].getBoundingClientRect().top; var availHeight = maxBottom - wrapTop; if ( availHeight < 160 || availWidth < 160 ) { return; } var aspects = getPortfolioItemAspectRatios($wrap); resetPortfolioItemSizes($wrap); $wrap.css({ display: 'flex', flexWrap: 'wrap', height: 'auto', gap: gapValue + 'px', alignItems: 'flex-end', justifyContent: 'center' }); var captionHeight = measurePortfolioCaptionHeight($wrap); var imageHeight = findEqualPortfolioImageHeight(aspects, gapValue, availWidth, availHeight, captionHeight); applyPortfolioEqualHeightLayout($wrap, imageHeight, aspects); for ( var fitPass = 0; fitPass < 4; fitPass++ ) { var usedHeight = $wrap[0].getBoundingClientRect().height; if ( usedHeight <= availHeight + 1 ) { break; } imageHeight = Math.max(40, Math.floor(imageHeight * (availHeight / usedHeight))); applyPortfolioEqualHeightLayout($wrap, imageHeight, aspects); } $portfolio.css({ minHeight: availHeight + paddingBottom + ($wrap[0].getBoundingClientRect().top - $portfolio[0].getBoundingClientRect().top), maxHeight: maxBottom - $portfolio[0].getBoundingClientRect().top }); if ( $wrap[0].getBoundingClientRect().bottom <= maxBottom + 1 && window.innerWidth >= 768 ) { $('body.is-projects-page').css('overflow', 'hidden'); } else { $('body.is-projects-page').css('overflow', ''); } } $(window).on('resize.portfolioFit orientationchange.portfolioFit', function() { clearTimeout(portfolioFitResizeTimer); portfolioFitResizeTimer = setTimeout(layoutPortfolioFit, 100); }); var coverBackgroundDuration = 600; var coverBackgroundTimer = null; var coverRevealed = false; var coverRevealAnimating = false; var coverRevealDuration = 600; function clearCoverBackgroundTimer() { if ( coverBackgroundTimer ) { clearTimeout(coverBackgroundTimer); coverBackgroundTimer = null; } } function forceCoverBackgroundReflow(element) { return element.offsetHeight; } function beginCoverBackgroundTransition() { $('body').addClass('cover-background-transition'); } function endCoverBackgroundTransition(callback) { coverBackgroundTimer = setTimeout(function() { coverBackgroundTimer = null; $('body').removeClass('cover-background-transition'); if ( callback ) { callback(); } }, coverBackgroundDuration); } function getDominantColorFromImage(img, region) { try { var canvas = document.createElement('canvas'); var sampleSize = 32; var sourceWidth = img.naturalWidth; var sourceHeight = img.naturalHeight; var cropX; var cropY; var cropWidth; var cropHeight; if ( region ) { cropX = sourceWidth * region.x; cropY = sourceHeight * region.y; cropWidth = sourceWidth * region.width; cropHeight = sourceHeight * region.height; } else { var cropScale = 0.5; cropWidth = sourceWidth * cropScale; cropHeight = sourceHeight * cropScale; cropX = (sourceWidth - cropWidth) / 2; cropY = (sourceHeight - cropHeight) / 2; } canvas.width = sampleSize; canvas.height = sampleSize; var context = canvas.getContext('2d'); context.drawImage( img, cropX, cropY, cropWidth, cropHeight, 0, 0, sampleSize, sampleSize ); var pixels = context.getImageData(0, 0, sampleSize, sampleSize).data; var redTotal = 0; var greenTotal = 0; var blueTotal = 0; var count = 0; for ( var i = 0; i < pixels.length; i += 4 ) { if ( pixels[i + 3] < 125 ) { continue; } if ( pixels[i] > 245 && pixels[i + 1] > 245 && pixels[i + 2] > 245 ) { continue; } redTotal += pixels[i]; greenTotal += pixels[i + 1]; blueTotal += pixels[i + 2]; count++; } if ( !count ) { return null; } return { r: Math.round(redTotal / count), g: Math.round(greenTotal / count), b: Math.round(blueTotal / count) }; } catch ( error ) { return null; } } function getCoverBrandingTextColor(img) { var sample = getDominantColorFromImage(img, { x: 0, y: 0.62, width: 0.42, height: 0.38 }); if ( !sample ) { return '#ffffff'; } var luminance = (0.299 * sample.r + 0.587 * sample.g + 0.114 * sample.b) / 255; return luminance > 0.58 ? '#1a1a1a' : '#ffffff'; } function updateCoverBrandingColor(img) { var $branding = $('.cover-branding'); if ( !$branding.length || !img || !img.complete || !img.naturalWidth ) { return; } var textColor = getCoverBrandingTextColor(img); $branding.css('color', textColor); } function isNavigatingToCoverIntro() { var targetPath = normalizeProjectPath(window.location.pathname); var fromPath = navSourcePath; navSourcePath = null; if ( targetPath !== '/' || !fromPath || fromPath === '/' ) { return false; } return $('.page .page__content .cover').length === 0; } function runSidebarExitAnimation(callback) { if ( !window.matchMedia('(min-width: 1024px)').matches ) { if ( callback ) { callback(); } return; } var $body = $('body'); $body.addClass('sidebar-exit-animating'); window.requestAnimationFrame(function() { $body.addClass('sidebar-exit-active'); window.setTimeout(function() { if ( callback ) { callback(); } }, coverRevealDuration); }); } function finishCoverRevealAnimation() { $('body').removeClass('cover-reveal-animating'); coverRevealAnimating = false; updateCoverBackground(); } function finishCoverCollapseAnimation($coverImage) { $('body') .removeClass('cover-collapse-animating') .addClass('cover-intro'); coverRevealAnimating = false; resetCoverBackground(false); updateCoverBrandingColor($coverImage); } function isCoverIntro() { return $('body').hasClass('cover-intro'); } function isCoverRevealed() { return $('body').hasClass('cover-revealed') && isCoverPage(); } function revealCoverLayout(animated) { if ( !isCoverIntro() || coverRevealAnimating ) { return false; } coverRevealed = true; var $body = $('body'); coverRevealAnimating = true; $body.removeClass('cover-intro').addClass('cover-reveal-animating'); window.requestAnimationFrame(function() { $body.addClass('cover-revealed'); window.setTimeout(function() { finishCoverRevealAnimation(); }, coverRevealDuration); }); return true; } function hideCoverLayout(animated) { if ( !isCoverRevealed() || coverRevealAnimating ) { return false; } coverRevealed = false; var $body = $('body'); var $coverImage = $('.page__content .cover__image')[0]; coverRevealAnimating = true; $body.addClass('cover-collapse-animating'); window.requestAnimationFrame(function() { $body.removeClass('cover-revealed'); window.setTimeout(function() { finishCoverCollapseAnimation($coverImage); }, coverRevealDuration); }); return true; } function resetCoverIntroState() { coverRevealed = false; coverRevealAnimating = false; $('body') .removeClass('cover-intro cover-revealed cover-reveal-animating cover-collapse-animating sidebar-exit-animating sidebar-exit-active'); } function softenCoverBackgroundColor(color) { var mix = 0.35; return { r: Math.round(color.r * mix + 255 * (1 - mix)), g: Math.round(color.g * mix + 255 * (1 - mix)), b: Math.round(color.b * mix + 255 * (1 - mix)) }; } function colorToRgbString(color) { return 'rgb(' + color.r + ', ' + color.g + ', ' + color.b + ')'; } function resetCoverBackground(animated) { var $body = $('body'); if ( animated && $body.hasClass('cover-page-active') ) { clearCoverBackgroundTimer(); beginCoverBackgroundTransition(); $body.css('background-color', '#ffffff'); endCoverBackgroundTransition(function() { $body .removeClass('cover-page-active') .css('background-color', ''); }); return; } if ( coverBackgroundTimer ) { return; } $body .removeClass('cover-page-active cover-background-transition') .css('background-color', ''); } function applyCoverBackgroundFromImage(img, animated) { if ( !img || !img.complete || !img.naturalWidth ) { resetCoverBackground(false); return; } var dominantColor = getDominantColorFromImage(img); if ( !dominantColor ) { resetCoverBackground(false); return; } var backgroundColor = softenCoverBackgroundColor(dominantColor); var tint = colorToRgbString(backgroundColor); var $body = $('body'); clearCoverBackgroundTimer(); $body.addClass('cover-page-active'); if ( animated === false ) { $body.css('background-color', tint); return; } beginCoverBackgroundTransition(); $body.css('background-color', '#ffffff'); forceCoverBackgroundReflow($body[0]); $body.css('background-color', tint); endCoverBackgroundTransition(); } function updateCoverBackground() { if ( isCoverIntro() ) { resetCoverBackground(false); return; } var $coverImage = $('.page__content .cover__image'); if ( !$coverImage.length ) { resetCoverBackground(false); return; } var coverImage = $coverImage[0]; if ( coverImage.complete && coverImage.naturalWidth ) { applyCoverBackgroundFromImage(coverImage, true); } else { $coverImage.one('load error', function() { applyCoverBackgroundFromImage(coverImage, true); }); } } function fadeCoverBackgroundOnLeave() { var newPath = normalizeProjectPath(window.location.pathname); var leavingCover = $('.page__content .cover').length > 0 && newPath !== '/'; if ( leavingCover ) { resetCoverBackground(true); } } function updateActiveLinks() { var currentPath = normalizeProjectPath(navTarget || window.location.pathname); $('.active-link').removeClass('active-link'); $('.menu .menu__list__item__link').each(function() { var linkPath = normalizeProjectPath($(this).attr('href')); var isActive = false; if ( linkPath === '/projects' ) { isActive = currentPath === '/projects' || currentPath.indexOf('/project/') === 0; } else { isActive = currentPath === linkPath; } if ( isActive ) { $(this).addClass('active-link'); } }); } function openFirstGalleryLightboxFromThumbnails() { if ( galleryLightboxAnimating || isGalleryLightboxOpen() ) { return false; } var $firstLink = $('.page__content .gallery--grid .gallery__item__link').first(); if ( !$firstLink.length ) { return false; } openGalleryLightbox($firstLink.attr('href'), $firstLink); return true; } function navigateToNextProjectFromThumbnails() { if ( galleryLightboxAnimating || isGalleryLightboxOpen() || !isProjectGalleryPage() ) { return false; } var nextProjectUrl = getNextProjectUrl(); if ( !nextProjectUrl ) { return false; } navTarget = nextProjectUrl; pushPageState(nextProjectUrl); return true; } function navigateToPreviousProjectFromThumbnails() { if ( galleryLightboxAnimating || isGalleryLightboxOpen() || !isProjectGalleryPage() ) { return false; } var previousProjectUrl = getPreviousProjectUrl(); if ( !previousProjectUrl ) { return false; } navTarget = previousProjectUrl; pushPageState(previousProjectUrl); return true; } function openFirstGalleryLightboxOnPage() { if ( !galleryLightboxOpenOnLoad ) { return; } galleryLightboxOpenOnLoad = false; openFirstGalleryLightboxFromThumbnails(); } function navigateToNextProject() { if ( galleryLightboxAnimating || !isGalleryLightboxOpen() ) { return; } var nextProjectUrl = getNextProjectUrl(); if ( !nextProjectUrl ) { return; } galleryLightboxOpenOnLoad = true; closeGalleryLightbox(true, function() { navTarget = nextProjectUrl; pushPageState(nextProjectUrl); }); } function closeLightboxOrNextProject() { if ( isProjectGalleryPage() ) { navigateToNextProject(); } else { closeGalleryLightbox(true); } } $(document).on('click', '.gallery--grid .gallery__item__link', function(event) { event.preventDefault(); openGalleryLightbox($(this).attr('href'), $(this)); }); $(document).on('click', '.gallery-lightbox__overlay', function() { closeGalleryLightbox(false); }); $(document).on('click', '.gallery-lightbox__viewport .gallery-lightbox__image', function() { if ( galleryLightboxDidSwipe ) { return; } closeGalleryLightbox(false); }); $galleryLightbox.on('touchstart', '.gallery-lightbox__viewport', function(event) { galleryLightboxTouchStartX = event.originalEvent.touches[0].clientX; galleryLightboxTouchStartY = event.originalEvent.touches[0].clientY; galleryLightboxDidSwipe = false; }); $galleryLightbox.on('touchend', '.gallery-lightbox__viewport', function(event) { var touchEndX = event.originalEvent.changedTouches[0].clientX; var touchEndY = event.originalEvent.changedTouches[0].clientY; var deltaX = touchEndX - galleryLightboxTouchStartX; var deltaY = touchEndY - galleryLightboxTouchStartY; if ( Math.abs(deltaX) < 50 && Math.abs(deltaY) < 50 ) { return; } galleryLightboxDidSwipe = true; if ( Math.abs(deltaY) > Math.abs(deltaX) && deltaY < 0 ) { closeGalleryLightbox(true); } else if ( Math.abs(deltaY) > Math.abs(deltaX) && deltaY > 0 ) { closeLightboxOrNextProject(); } else if ( Math.abs(deltaX) >= 50 ) { if ( deltaX < 0 ) { navigateGalleryLightbox(1); } else { navigateGalleryLightbox(-1); } } setTimeout(function() { galleryLightboxDidSwipe = false; }, 400); }); $(document).on('click', '.cover-intro .cover', function(event) { event.preventDefault(); revealCoverLayout(true); }); $(document).on('keydown', function(event) { if ( isGalleryLightboxOpen() ) { if ( event.key === 'Escape' ) { closeGalleryLightbox(false); } else if ( event.key === 'ArrowUp' ) { event.preventDefault(); closeGalleryLightbox(true); } else if ( event.key === 'ArrowDown' ) { event.preventDefault(); closeLightboxOrNextProject(); } else if ( event.key === 'ArrowLeft' ) { event.preventDefault(); navigateGalleryLightbox(-1); } else if ( event.key === 'ArrowRight' ) { event.preventDefault(); navigateGalleryLightbox(1); } return; } if ( isCoverIntro() && (event.key === 'ArrowUp' || event.key === 'ArrowDown' || event.key === 'ArrowLeft' || event.key === 'ArrowRight') ) { event.preventDefault(); revealCoverLayout(true); return; } if ( isCoverRevealed() && event.key === 'ArrowLeft' ) { event.preventDefault(); hideCoverLayout(true); return; } if ( event.key === 'ArrowLeft' && navigateUpHierarchy() ) { event.preventDefault(); } else if ( event.key === 'ArrowRight' ) { if ( navigateDownHierarchy() ) { event.preventDefault(); } else if ( hasPageGridGallery() && openFirstGalleryLightboxFromThumbnails() ) { event.preventDefault(); } } else if ( isMainNavigationContext() ) { if ( event.key === 'ArrowDown' && navigateMainPage(1) ) { event.preventDefault(); } else if ( event.key === 'ArrowUp' && navigateMainPage(-1) ) { event.preventDefault(); } } else if ( isProjectGalleryPage() ) { if ( event.key === 'ArrowRight' && openFirstGalleryLightboxFromThumbnails() ) { event.preventDefault(); } else if ( event.key === 'ArrowDown' && navigateToNextProjectFromThumbnails() ) { event.preventDefault(); } else if ( event.key === 'ArrowUp' && navigateToPreviousProjectFromThumbnails() ) { event.preventDefault(); } } }); $(document).on('touchstart', '.page__content', function(event) { if ( isGalleryLightboxOpen() ) { return; } galleryThumbTouchStartX = event.originalEvent.touches[0].clientX; galleryThumbTouchStartY = event.originalEvent.touches[0].clientY; portfolioTouchStartX = galleryThumbTouchStartX; portfolioTouchStartY = galleryThumbTouchStartY; }); $(document).on('touchend', '.page__content', function(event) { if ( isGalleryLightboxOpen() ) { return; } if ( isCoverIntro() ) { revealCoverLayout(true); return; } var touchEndX = event.originalEvent.changedTouches[0].clientX; var touchEndY = event.originalEvent.changedTouches[0].clientY; var deltaX = touchEndX - galleryThumbTouchStartX; var deltaY = touchEndY - galleryThumbTouchStartY; if ( isCoverRevealed() ) { if ( Math.abs(deltaX) >= 50 && Math.abs(deltaX) > Math.abs(deltaY) && deltaX < 0 ) { hideCoverLayout(true); } return; } if ( Math.abs(deltaX) < 50 && Math.abs(deltaY) < 50 ) { return; } if ( hasPageGridGallery() ) { if ( Math.abs(deltaY) > Math.abs(deltaX) && deltaY > 0 ) { navigateToNextProjectFromThumbnails(); } else if ( Math.abs(deltaY) > Math.abs(deltaX) && deltaY < 0 ) { navigateToPreviousProjectFromThumbnails(); } else if ( Math.abs(deltaX) >= 50 && Math.abs(deltaX) > Math.abs(deltaY) ) { if ( deltaX < 0 ) { navigateUpHierarchy(); } else { openFirstGalleryLightboxFromThumbnails(); } } return; } if ( !isMainNavigationContext() ) { return; } deltaX = touchEndX - portfolioTouchStartX; deltaY = touchEndY - portfolioTouchStartY; if ( Math.abs(deltaX) < 50 && Math.abs(deltaY) < 50 ) { return; } if ( Math.abs(deltaY) > Math.abs(deltaX) && deltaY > 0 ) { navigateMainPage(1); } else if ( Math.abs(deltaY) > Math.abs(deltaX) && deltaY < 0 ) { navigateMainPage(-1); } else if ( Math.abs(deltaX) >= 50 && Math.abs(deltaX) > Math.abs(deltaY) && deltaX < 0 ) { navigateUpHierarchy(); } else if ( Math.abs(deltaX) >= 50 && Math.abs(deltaX) > Math.abs(deltaY) && deltaX > 0 ) { navigateDownHierarchy(); } }); var skipInitialStateChange = true; var pageLoadRequestId = 0; function hasLoadedPageContent($loader) { return $loader.find('.page__content').length > 0; } History.Adapter.bind(window,'statechange',function(){ if ( skipInitialStateChange ) { skipInitialStateChange = false; return; } var navigatingToCoverIntro = isNavigatingToCoverIntro(); var loadUrl = getAjaxPageLoadUrl(); var thisRequestId = ++pageLoadRequestId; var $pageLoader = $('.page-loader'); fadeCoverBackgroundOnLeave(); $('body').addClass('loading'); scheduleLoadingFallback(); $pageLoader.empty(); function swapPageContent() { if ( !hasLoadedPageContent($pageLoader) ) { handleFailedPageLoad(loadUrl); return; } $( 'body, html' ).scrollTop(0); $('.page .page__content').remove(); $pageLoader.find('.page__content').appendTo('.page'); $('body').attr('data-page-url', window.location.pathname); navTarget = $('body').attr('data-page-url'); var nextTitle = $('.page__content').attr('data-page-title'); if ( nextTitle ) { docTitle = nextTitle; document.title = docTitle; } try { pageFunctions(); } catch ( error ) { revealPageContent(); } $('body').removeClass('sidebar-exit-animating sidebar-exit-active'); } function onPageLoadComplete(status) { if ( thisRequestId !== pageLoadRequestId ) { return false; } if ( status !== 'success' || !hasLoadedPageContent($pageLoader) ) { handleFailedPageLoad(loadUrl); return false; } return true; } if ( navigatingToCoverIntro ) { var loadDone = false; var exitDone = false; function trySwapToCoverIntro() { if ( !loadDone || !exitDone ) { return; } swapPageContent(); } runSidebarExitAnimation(function() { exitDone = true; trySwapToCoverIntro(); }); loadAjaxPageContent(loadUrl, $pageLoader, function(status) { loadDone = onPageLoadComplete(status) === true; trySwapToCoverIntro(); }); } else { var transitionTime = 400; loadAjaxPageContent(loadUrl, $pageLoader, function(status) { if ( onPageLoadComplete(status) !== true ) { return; } $( 'body, html' ).scrollTop(0); window.setTimeout(function() { if ( thisRequestId !== pageLoadRequestId ) { return; } swapPageContent(); }, transitionTime); }); } }); if ( $('body').hasClass('ajax-loading') ) { $(document).on('click', 'a', function (event){ event.preventDefault(); if ( $(this).hasClass('js-weixin-open') ) { return; } var thisTarget = $(this).attr('href'); if ($(this).hasClass('js-no-ajax') || thisTarget.indexOf('#') >= 0 || thisTarget.indexOf('mailto:') >= 0 || thisTarget.indexOf('tel:') >= 0) { window.location = thisTarget; } else if ( $(this).is('.gallery__item__link') ) { } else if ( thisTarget.indexOf('http') >= 0 ) { window.open(thisTarget, '_blank'); } else { navSourcePath = normalizeProjectPath($('body').attr('data-page-url') || window.location.pathname); navTarget = thisTarget; pushPageState(thisTarget); } }); } function pageFunctions() { var $cover = $('.page__content .cover'); if ( $cover.length ) { $('body') .addClass('is-cover-page cover-intro') .removeClass('cover-revealed cover-reveal-animating'); coverRevealed = false; coverRevealAnimating = false; $('.page').addClass('page--cover'); randomizeCoverImage(function(success, coverImage) { if ( !success || !coverImage ) { revealPageContent(); return; } $(coverImage).imagesLoaded(function() { updateCoverBrandingColor(coverImage); updateCoverBackground(); revealPageContent(); }); }); } else { resetCoverIntroState(); $('body').removeClass('is-cover-page is-projects-page').css('overflow', ''); $('.page').removeClass('page--cover'); var $pageContent = $('.page__content'); if ( !$pageContent.length ) { revealPageContent(); return; } var $firstImage = $pageContent.find('img:first'); if ( $firstImage.length ) { $firstImage.imagesLoaded(function() { revealPageContent(); }); } else { revealPageContent(); } window.setTimeout(function() { if ( $('body').hasClass('loading') ) { revealPageContent(); } }, 3000); } updateActiveLinks(); if ( !$cover.length ) { updateCoverBackground(); } if ( window.Waypoint && typeof Waypoint.destroyAll === 'function' ) { Waypoint.destroyAll(); } var galleryCount = 0; $('.gallery').each( function() { var $this = $(this); galleryCount++; var thisId = 'gallery-' + galleryCount; $this.attr('id', thisId); var galleryCols = $this.attr('data-columns'); $this.append('<div class="gallery__wrap"></div>'); $this.children('img').each( function() { $(this).appendTo('#' + thisId + ' .gallery__wrap'); }); $this.find('.gallery__wrap img').each(function() { var imageSrc = $(this).attr('src'); $(this) .on('error', function() { $(this).closest('.gallery').addClass('gallery--on'); }) .wrap('<a href="' + imageSrc + '" class="gallery__item__link"></a>') .parent() .wrap('<div class="gallery__item"></div>'); }); var showGallery = function() { $this.addClass('gallery--on'); }; $this.imagesLoaded(function() { if ( galleryCols === '1' ) { $this.addClass('gallery--carousel'); $this.children('.gallery__wrap').addClass('owl-carousel'); $this.children('.gallery__wrap').owlCarousel({ items: 1, loop: true, mouseDrag: false, touchDrag: true, pullDrag: false, dots: true, autoplay: false, autoplayTimeout: 6000, autoHeight: true, animateOut: 'fadeOut' }); var waypoint1 = new Waypoint({ element: document.getElementById(thisId), handler: function(direction) { if ( direction === 'down') { $this.children('.gallery__wrap').trigger('stop.owl.autoplay'); } if ( direction === 'up') { $this.children('.gallery__wrap').trigger('play.owl.autoplay'); } }, offset: '-100%' }); var waypoint2 = new Waypoint({ element: document.getElementById(thisId), handler: function(direction) { if ( direction === 'down') { $this.children('.gallery__wrap').trigger('play.owl.autoplay'); } if ( direction === 'up') { $this.children('.gallery__wrap').trigger('stop.owl.autoplay'); } }, offset: '100%' }); } else { $this.addClass('gallery--grid'); $this.children('.gallery__wrap').masonry({ itemSelector: '.gallery__item', transitionDuration: 0 }); openFirstGalleryLightboxOnPage(); } showGallery(); }); window.setTimeout(showGallery, 5000); }); if ( galleryLightboxOpenOnLoad && !$('.page__content .gallery--grid').length ) { galleryLightboxOpenOnLoad = false; } $('.single p > img').each( function() { var thisP = $(this).parent('p'); $(this).insertAfter(thisP); $(this).wrapAll('<div class="image-wrap"></div>'); thisP.remove(); }); $('.single iframe').each( function() { if ( $(this).attr('src').indexOf('youtube') >= 0 || $(this).attr('src').indexOf('vimeo') >= 0 ) { var width = $(this).attr('width'); var height = $(this).attr('height'); var ratio = (height/width)*100; $(this).wrapAll('<div class="video-wrap"><div class="video" style="padding-bottom:' + ratio + '%;"></div></div>'); } }); $('.single table').each(function () { $(this).wrapAll('<div class="table-wrap"></div>'); }); } pageFunctions(); $(document).on('click', '.js-menu-toggle', function (){ if ( $('body').hasClass('menu--open') ) { $('body').removeClass('menu--open'); } else { $('body').addClass('menu--open'); } }); $(document).on('click', '.menu__list__item__link', function (){ if ( $('.menu').hasClass('menu--open') ) { $('.menu').removeClass('menu--open'); } }); $(document).on('submit', '#contact-form', function (e) { $('.contact-form__item--error').removeClass('contact-form__item--error'); var emailField = $('.contact-form__input[name="email"]'); var nameField = $('.contact-form__input[name="name"]'); var messageField = $('.contact-form__textarea[name="message"]'); var gotchaField = $('.contact-form__gotcha'); if ( emailField.val() === '' ) { emailField.closest('.contact-form__item').addClass('contact-form__item--error'); } if ( nameField.val() === '' ) { nameField.closest('.contact-form__item').addClass('contact-form__item--error'); } if ( messageField.val() === '' ) { messageField.closest('.contact-form__item').addClass('contact-form__item--error'); } if ( emailField.val() !== '' && nameField.val() !== '' && messageField.val() !== '' && gotchaField.val().length === 0 ) { } else { e.preventDefault(); } }); var $weixinModal = $('#weixin-modal'); if ( $weixinModal.length ) { var $weixinCopyButtons = $weixinModal.find('.js-weixin-copy'); var weixinCopyDefaultLabels = $weixinCopyButtons.map(function() { return $(this).text(); }).get(); function openWeixinModal() { $weixinModal .addClass('weixin-modal--open') .attr('aria-hidden', 'false'); $('body').addClass('weixin-modal-active'); } function closeWeixinModal() { $weixinModal .removeClass('weixin-modal--open') .attr('aria-hidden', 'true'); $('body').removeClass('weixin-modal-active'); $weixinCopyButtons.each(function(index) { var $button = $(this); $button .removeClass('weixin-modal__copy--copied weixin-modal__id--copied') .text(weixinCopyDefaultLabels[index]); }); $weixinModal.find('.weixin-modal__id').removeClass('weixin-modal__id--copied'); } function legacyCopyWeixinId(weixinId) { var textarea = document.createElement('textarea'); textarea.value = weixinId; textarea.setAttribute('readonly', ''); textarea.style.position = 'fixed'; textarea.style.top = '0'; textarea.style.left = '0'; textarea.style.width = '2em'; textarea.style.height = '2em'; textarea.style.padding = '0'; textarea.style.border = 'none'; textarea.style.outline = 'none'; textarea.style.boxShadow = 'none'; textarea.style.background = 'transparent'; textarea.style.opacity = '0'; document.body.appendChild(textarea); textarea.focus(); textarea.select(); textarea.setSelectionRange(0, weixinId.length); var copied = false; try { copied = document.execCommand('copy'); } catch ( error ) {} document.body.removeChild(textarea); return copied; } function showWeixinCopiedState() { var copiedLabel = $weixinModal.find('.js-weixin-copy').first().attr('data-copied-label') || 'Copied'; $weixinModal.find('.weixin-modal__id').addClass('weixin-modal__id--copied'); $weixinModal.find('.weixin-modal__copy').each(function() { $(this) .addClass('weixin-modal__copy--copied') .text(copiedLabel); }); } function copyWeixinId() { var weixinId = $.trim($weixinModal.find('.weixin-modal__id').first().text()); if ( !weixinId ) { return; } function finishCopy(success) { if ( success ) { showWeixinCopiedState(); } } if ( legacyCopyWeixinId(weixinId) ) { finishCopy(true); return; } if ( navigator.clipboard && navigator.clipboard.writeText ) { navigator.clipboard.writeText(weixinId) .then(function() { finishCopy(true); }) .catch(function() { finishCopy(legacyCopyWeixinId(weixinId)); }); return; } finishCopy(false); } $(document).on('click', '.js-weixin-open', function(event) { event.preventDefault(); openWeixinModal(); }); $weixinModal.on('click', '.weixin-modal__overlay, .weixin-modal__close', function() { closeWeixinModal(); }); $weixinModal.on('click', '.js-weixin-copy', function(event) { event.preventDefault(); copyWeixinId(); }); $weixinModal.on('click', '.weixin-modal__panel', function(event) { event.stopPropagation(); }); $(document).on('keydown', function(event) { if ( event.key === 'Escape' && $weixinModal.hasClass('weixin-modal--open') ) { closeWeixinModal(); } }); } }(jQuery));